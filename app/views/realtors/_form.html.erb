<%= form_for(realtor) do |f| %>
  <% if realtor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(realtor.errors.count, "error") %> prohibited this realtor from being saved:</h2>

      <ul>
      <% realtor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">

      <p>物件名</p>
      <p><%= f.text_field :house %></p>
      <p>賃料</p>
      <p> <%= f.text_field :price %>円</p>
      <p>住所</p>
      <p><%= f.text_field :address %></p>
      <p>築年数</p>
      <p><%= f.text_field :age %>年</p>
      <p>備考</p>
      <p><%= f.text_area :remarkes %></p>
      <p><%= f.submit %></p>
    <% end %>


  </div>
